name: CI

on: [push]

jobs:
  build:

    runs-on: macOS-latest

    steps:
      - name: Test
        run: echo "${{ job.status == 'failure' && 'fail' || job.status == 'cancelled' && 'cancel' || job.status == 'success' && 'success!' }}"
